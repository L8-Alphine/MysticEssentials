package com.alphine.mysticessentials.vault;

public final class VaultMeta {
    /** Custom base name (without the " - #n" suffix). If null/blank => config default. */
    public String customBaseName;

    /** Namespaced item id for selector icon. If null/blank => config default. */
    public String displayItemId;
}
